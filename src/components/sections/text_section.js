export default (header = '', subtitle = '') => {
    return `<div class="col-12 mt-5 my-4 py-4">
    <h4 class="text-center color-gray-darker">${header}</h4>
    <p class="color-gray-dark px-5 text-center" >${subtitle}</p>
    </div>`
}